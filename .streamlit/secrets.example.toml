# Copy this file to `.streamlit/secrets.toml` and fill in your key.
# This file is safe to commit; the real `secrets.toml` is ignored by .gitignore.

# OpenAI configuration
OPENAI_API_KEY = "sk-xxxxx_your_key_here"
